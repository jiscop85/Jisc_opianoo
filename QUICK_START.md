# 🚀 راهنمای سریع اجرا

## مرحله 1: نصب وابستگی‌ها

### Windows (PowerShell یا CMD)

```powershell
# رفتن به پوشه پروژه
cd PianoMasterTutor

# ایجاد محیط مجازی (اختیاری اما توصیه می‌شود)
python -m venv venv

# فعال کردن محیط مجازی
venv\Scripts\activate

# نصب وابستگی‌ها
pip install -r requirements.txt
```

### Linux/Mac

```bash
# رفتن به پوشه پروژه
cd PianoMasterTutor

# ایجاد محیط مجازی
python3 -m venv venv

# فعال کردن محیط مجازی
source venv/bin/activate

# نصب وابستگی‌ها
pip install -r requirements.txt
```

## مرحله 2: دانلود SoundFont (اختیاری اما توصیه می‌شود)

برای صدای بهتر پیانو:

1. فایل `SalamanderGrandPiano.sf2` را از این آدرس دانلود کنید:
   - https://musical-artifacts.com/artifacts/794
   - یا جستجو کنید: "Salamander Grand Piano SF2"

2. فایل را در پوشه زیر قرار دهید:
   ```
   PianoMasterTutor/assets/soundfonts/SalamanderGrandPiano.sf2
   ```

**نکته**: اگر SoundFont نصب نکنید، برنامه با صدای پیش‌فرض FluidSynth کار می‌کند.

## مرحله 3: اجرای برنامه

```bash
python main.py
```

یا:

```bash
python3 main.py
```

## مرحله 4: اولین استفاده

### 1. ثبت‌نام/ورود
- برنامه باز می‌شود و از شما می‌خواهد ثبت‌نام یا ورود کنید
- یک حساب کاربری جدید بسازید

### 2. کالیبراسیون (مهم!)
- بعد از ورود، روی دکمه "کالیبراسیون" کلیک کنید
- چهار نقطه گوشه کیبورد پیانو خود را در تصویر وبکم مشخص کنید:
  1. نقطه بالا چپ
  2. نقطه بالا راست
  3. نقطه پایین راست
  4. نقطه پایین چپ
- روی "اتمام کالیبراسیون" کلیک کنید

### 3. اضافه کردن درس
- فایل‌های MIDI را در پوشه‌های زیر قرار دهید:
  - `assets/midi_files/beginner/` - برای درس‌های مبتدی
  - `assets/midi_files/intermediate/` - برای درس‌های متوسط
  - `assets/midi_files/advanced/` - برای درس‌های پیشرفته

### 4. شروع تمرین
- یک درس را از لیست انتخاب کنید
- روی "شروع درس" کلیک کنید
- شروع به نواختن کنید!

## عیب‌یابی

### مشکل: `ModuleNotFoundError`
```bash
# مطمئن شوید که همه وابستگی‌ها نصب شده‌اند
pip install -r requirements.txt
```

### مشکل: وبکم کار نمی‌کند
- مطمئن شوید که وبکم به کامپیوتر متصل است
- بررسی کنید که هیچ برنامه دیگری از وبکم استفاده نمی‌کند
- وبکم را در Device Manager بررسی کنید

### مشکل: صدا پخش نمی‌شود
- اگر SoundFont نصب کرده‌اید، مسیر آن را در `config.py` بررسی کنید
- برنامه بدون SoundFont هم کار می‌کند اما با صدای پیش‌فرض

### مشکل: دست تشخیص داده نمی‌شود
- نور محیط را بررسی کنید
- فاصله از وبکم را تنظیم کنید (حدود 50-80 سانتی‌متر)
- کالیبراسیون را دوباره انجام دهید

## ویژگی‌های برنامه

- 🎹 پیانو مجازی 88 کلاویه
- 📹 ردیابی دست در زمان واقعی
- 👆 تشخیص انگشت استفاده شده
- 🏆 سیستم امتیاز و دستاورد
- 📊 گزارش‌های تحلیلی
- 🎨 تم روشن/تاریک
- 📹 ضبط جلسه تمرین

## پشتیبانی

اگر مشکلی داشتید:
1. فایل لاگ را بررسی کنید: `data/piano_tutor.log`
2. خطاها را در کنسول بررسی کنید
3. مطمئن شوید که Python 3.8+ نصب است

---

**موفق باشید! 🎵**

